spring.application.name=crud

# Configuración de la base de datos H2
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=wulises
spring.datasource.password=haruxwulisexmakoto
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Habilitar la consola de H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Crear las tablas automáticamente con Hibernate (esto debería recrear las tablas)
spring.jpa.hibernate.ddl-auto=create

# Crear la tabla antes de que corra data.sql
spring.jpa.defer-datasource-initialization=true